// Público (NUNCA metas secretos aquí)
export const CONFIG = {
  PUBLIC_URL: (typeof location !== 'undefined') ? location.origin : '',
  // YouTube por defecto (puedes cambiarlo desde la UI)
  DEFAULT_YT: 'dQw4w9WgXcQ',
  I18N_DEFAULT: 'es',
};